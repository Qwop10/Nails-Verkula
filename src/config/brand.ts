/**
 * config/brand.ts
 * Единственное место с клиент-специфичными данными.
 * Для нового клиента/ниши меняем ТОЛЬКО этот файл (+ theme/flow).
 */

export const BRAND = {
  /** Название и подпись (для текстовых мест). */
  name: 'Nails Verkula',
  tagline: 'студия маникюра',

  /** Логотип на главной (файл в /public). Пусто → показываем текстовый логотип. */
  logo: '',

  /** Telegram id мастера — у него открывается кабинет (1 мастер). */
  masterTelegramIds: [763224120, 628854840, 1470903380] as number[],

  /** Бронь (предоплата). Фиксированная, входит в стоимость процедуры. */
  booking: {
    /** Сумма брони, ₽. */
    fee: 500,
    /** За сколько часов до записи отмена ещё возвращает бронь. */
    refundHours: 24,
    /** Реквизиты для перевода брони (клиент платит вручную и присылает чек). */
    requisites: {
      card: '2202208812470012',
      bank: 'Сбер',
    },
  },
} as const;
