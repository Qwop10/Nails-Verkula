/**
 * server/content.mjs
 * Тексты, которые бот шлёт клиенту (под нишу — меняются здесь).
 */

// Адрес студии (заполнить через переменную окружения STUDIO_ADDRESS).
export const STUDIO_ADDRESS = (process.env.STUDIO_ADDRESS || '').trim();

export const MEMO_TEXT =
  `📋 <b>Памятка перед визитом</b>\n\n` +
  `💅 <b>Ногти:</b> приходите со «своим» покрытием — снятие старого включено в работу, отдельно ничего делать не нужно.\n\n` +
  `✋ <b>Кутикула:</b> не подстригайте и не отодвигайте кутикулу за 2–3 дня до визита — так обработка будет аккуратнее.\n\n` +
  `🧴 <b>Кожа рук:</b> в день визита не пользуйтесь жирным кремом и маслом для кутикулы.\n\n` +
  `⏰ <b>Время:</b> приходите за 5 минут до записи. Если опаздываете — напишите мастеру.\n\n` +
  `🎒 <b>С собой:</b> по желанию — референсы дизайна (фото).`;

export const CONTRA_TEXT =
  `⚠️ <b>Когда лучше перенести визит</b>\n\n` +
  `• Грибковые заболевания ногтей и кожи (до выздоровления)\n` +
  `• Открытые порезы, ранки, воспаления в зоне обработки\n` +
  `• Аллергия на гель-лак / материалы — предупредите мастера заранее\n\n` +
  `Если что-то из списка про вас — напишите мастеру до визита, подберём решение.`;
